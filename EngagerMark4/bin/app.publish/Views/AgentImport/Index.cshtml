
@{
    ViewBag.Title = "Import Agent";
}
@Html.Partial("_ListMessage")
@using (Html.BeginForm("Index", "AgentImport", FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @Html.AntiForgeryToken()
    <div class="panel panel-info">
        <div class="panel-heading">Import Agent</div>
        <div class="panel-body">
            <div class="form-horizontal">
                <fieldset>
                    <legend></legend>

                    <div class="form-group">
                        <label class="control-label col-sm-2">Agent Excel File</label>
                        <div class="col-sm-4" data-role="input-control">
                            <input style="margin-top:5px;" type="file" name="agentExcel" />
                        </div>
                    </div>
                </fieldset>
            </div>
        </div>
        <div class="panel-footer">
            <div class="col-sm-2"></div>
            <button type="submit" id="btnImportAgent" value="Import Agent" class="btn btn-primary">Import Agent</button>
        </div>
    </div>
}
@Html.Partial("_ProcessScreen")
<script src="~/Scripts/Custom/AgentImport/Index.js"></script>